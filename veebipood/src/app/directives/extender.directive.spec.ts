import { ExtenderDirective } from './extender.directive';

describe('ExtenderDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtenderDirective();
    expect(directive).toBeTruthy();
  });
});
